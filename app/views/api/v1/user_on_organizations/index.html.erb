<p id="notice"><%= notice %></p>

<h1>User On Organizations</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_on_organizations.each do |user_on_organization| %>
      <tr>
        <td><%= link_to 'Show', user_on_organization %></td>
        <td><%= link_to 'Edit', edit_user_on_organization_path(user_on_organization) %></td>
        <td><%= link_to 'Destroy', user_on_organization, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User On Organization', new_user_on_organization_path %>
