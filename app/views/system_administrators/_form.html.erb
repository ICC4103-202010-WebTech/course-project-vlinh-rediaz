<%= form_with(model: system_administrator, local: true) do |form| %>
  <% if system_administrator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(system_administrator.errors.count, "error") %> prohibited this system_administrator from being saved:</h2>

      <ul>
        <% system_administrator.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
