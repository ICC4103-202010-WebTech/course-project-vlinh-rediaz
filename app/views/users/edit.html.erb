<div class="row ml-0 mt-3">
  <h2>Edit "<%=@user.username%>" User</h2>
</div>
<div class="row ml-0 mt-2">
  <p>Please enter the required information to edit the "<%=@user.username%>" user:</p>
</div>
<%= render partial: "form",
           locals: {
               user: @user,
               submit_btn_text: "Update"
           } %>
<br>
<%= link_to "Back", :back, class: ["btn", "btn-secondary"] %> |
<%= link_to 'Show', @user %>
